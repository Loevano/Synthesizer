# Dev Log

Create one file per day for progress notes:
- what changed
- what worked / failed
- next steps

Use `./scripts/new-devlog-entry.sh` to generate a dated entry template.

Devlog entries are optional working notes. Do not use them instead of updating the main docs when behavior, architecture, or workflow changes.
